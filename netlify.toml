[build]
  command = "bash build.sh"
  publish = "site"
  ignore = "git diff --quiet $CACHED_COMMIT_REF $COMMIT_REF -- '*.md' docs/ mkdocs.yml mkdocs_hooks.py configs/multilingual_benchmarks.csv"

[build.environment]
  NODE_VERSION = "20"
  PYTHON_VERSION = "3.11"
