[build]
  base = "documents"
  command = "npm install && npm run build"
  publish = "documents/dist"
  ignore = "git diff --quiet $CACHED_COMMIT_REF $COMMIT_REF -- slides.md"

[build.environment]
  NODE_VERSION = "20"
